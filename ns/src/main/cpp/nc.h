//
// Created by boot on 4/20/21.
//

#ifndef NL_NC_H
#define NL_NC_H
#include <jni.h>
#include <string.h>
#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>
//#include <stdarg.h>
//#include <ctype.h>

#ifndef __cplusplus
#define max(a,b) (((a) > (b)) ? (a) : (b))
#define min(a,b) (((a) < (b)) ? (a) : (b))
#endif


//#ifndef NOMINMAX
//
//#ifndef max
//#define max(a,b)            (((a) > (b)) ? (a) : (b))
//#endif
//
//#ifndef min
//#define min(a,b)            (((a) < (b)) ? (a) : (b))
//#endif
//
//#endif  /* NOMINMAX */

#endif //NL_NC_H
